# Leave unset in dev — Vite proxies /api/* to http://localhost:8000.
# In production (e.g. Vercel), set this to your deployed backend origin.
# VITE_API_BASE_URL=https://blog-admin-portal-api.fly.dev
